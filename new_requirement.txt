torch==1.3.1

usage: main_simulated_CWT_clinical.py contain both train and test 
if test or train with CWT, then set CWT_train = Train, if only test on one dataset set to False 
if Test = True and epochs =0 then we only test and true = 0 
